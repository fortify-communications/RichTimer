source 'https://github.com/CocoaPods/Specs.git'
use_frameworks!

target 'RichTimer_Example', :exclusive => true do
  pod 'RichTimer', :path => '../'
end

target 'RichTimer_Tests', :exclusive => true do
  pod 'RichTimer', :path => '../'

  pod 'Quick', '~> 0.8'
  pod 'Nimble', '~> 3.0'
end
